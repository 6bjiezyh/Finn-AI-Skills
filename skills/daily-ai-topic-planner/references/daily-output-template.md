# Daily Output Template

Use this exact structure in Chinese.

## LongVideoIdeas

Provide exactly 3 items.

For each item include:

- `topic_angle`:
- `category`:
- `audience_pain`:
- `titles`:
  - 1)
  - 2)
  - 3)
- `content_outline`:
- `monetization_hook`:
- `cta`:
- `timeliness`:
- `difficulty`:
- `score`:

## ShortVideoIdeas

Provide exactly 7 items.

For each item include:

- `topic_angle`:
- `category`:
- `audience_pain`:
- `titles`:
  - 1)
  - 2)
  - 3)
- `content_outline`:
- `monetization_hook`:
- `cta`:
- `timeliness`:
- `difficulty`:
- `score`:

## PublishPriority

Provide exactly 3 recommendations:

- `long_video_today`: one item and why.
- `short_video_today_1`: one item and why.
- `short_video_today_2`: one item and why.

## ExpandedTopicPool20

Always include this section in every response.

Provide exactly 20 items with fixed category quotas:

- `AI自动化`: 6
- `Vibe Coding`: 4
- `AIGC`: 4
- `AI提效`: 3
- `AI变现案例`: 3

All 20 items must be AI-original ideas:

- do not copy source titles
- do not output simple rewrites of trending headlines
- each item must include a fresh, synthesized angle

For each item include:

- `topic_angle`:
- `category`:
- `audience_pain`:
- `titles`:
  - 1)
  - 2)
  - 3)
- `content_outline`:
- `monetization_hook`:
- `cta`:
- `timeliness`:
- `difficulty`:
- `score`:

## Consistency Check

Before finalizing output:

- verify 3 long + 7 short counts
- verify every item has all required fields
- verify each item maps to a conversion target
- verify no duplicates against provided recent topics
- verify `ExpandedTopicPool20` has exactly 20 items
- verify category counts are 6/4/4/3/3
- verify all 20 items are AI-original, not headline rewrites
